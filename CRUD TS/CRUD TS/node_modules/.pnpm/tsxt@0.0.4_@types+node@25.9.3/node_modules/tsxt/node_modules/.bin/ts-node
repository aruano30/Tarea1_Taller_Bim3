#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../ts-node/dist/bin.js" "$@"
else
  exec node  "$basedir/../../../ts-node/dist/bin.js" "$@"
fi
# cmd-shim-target=C:/Users/Informatica/Documents/CRUD TS/node_modules/.pnpm/tsxt@0.0.4_@types+node@25.9.3/node_modules/ts-node/dist/bin.js
