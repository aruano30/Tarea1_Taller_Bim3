#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../jest-cli/bin/jest.js" "$@"
else
  exec node  "$basedir/../../../jest-cli/bin/jest.js" "$@"
fi
# cmd-shim-target=C:/Users/Informatica/Documents/CRUD TS/node_modules/.pnpm/jest@29.5.0_@types+node@25._6e0f314c2835eee4bb8f32a565ceb1f3/node_modules/jest-cli/bin/jest.js
